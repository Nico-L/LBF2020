{%- for machine in lesMachines -%}
    {% set regExp = r/^four.*/g %}
    <div class="w-320px border rounded-lg {% include "components/classCartesMachines.njk" %}">
        <div class="bg-gray-100 rounded-t-lg">
            <img src="https://cms.labonnefabrique.fr{{machine.illustration[0].formats.small.url}}" class="rounded-t-lg" width="318"/>
            <div class="text-xl p-1 text-center font-bold capitalize mt-1 align-middle">{{machine.nom}}</div>
            <div class="text-base text-justify p-1 align-top rounded-br-lg listeMachines">{{machine.resume | safe}}</div>
        </div>
        <div class="rounded-b-lg">
            {%if not machine.Abonnement%}
                <div class="my-1 mx-2 text-right">
                    <span class="text-xl font-medium">{{machine.tarifHoraire}}</span>
                    <span class="text-base">&nbsp;€/h</span>
                </div>
            {% elif 'four' in machine.tag %}
                <div class="my-1 mx-2 text-right text-base font-medium">
                    tarif: nous contacter
                </div>
            {% else %}
                <div class="my-1 mx-2 text-right text-base font-medium">
                    Sur abonnement
                </div>
            {% endif %}
        </div>
    </div>
{%- endfor -%}